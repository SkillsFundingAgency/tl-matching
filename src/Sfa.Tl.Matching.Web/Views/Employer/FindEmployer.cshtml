@model Sfa.Tl.Matching.Models.ViewModel.FindEmployerViewModel
@{
    ViewData["Title"] = "Who is the employer?";
}

@section backButton{
    <a asp-route="PlacementInformationSave_Get" asp-route-id="@Model.OpportunityId" class="govuk-back-link">Back</a>
    <a asp-controller="Provider" asp-action="Start" class="govuk-link tl-float-right govuk-!-margin-top-3" id="tl-finish"> Finish and start again </a>
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        @if (!ViewData.ModelState.IsValid)
        {
            <div class="govuk-error-summary" aria-labelledby="error-summary-title" role="alert" tabindex="-1" data-module="error-summary">
                <h2 class="govuk-error-summary__title" id="error-summary-title">
                    This page has errors
                </h2>
                <div class="govuk-error-summary__body">
                    <ul class="govuk-list govuk-error-summary__list">
                        <li><a sfa-validation-for="EmployerName"></a></li>
                    </ul>
                </div>
            </div>
        }
        <form asp-action="FindEmployer" id="findEmployer" novalidate>
            <input asp-for="OpportunityId" type="hidden" />

            <div class="govuk-form-group" sfa-validation-for="EmployerName" sfa-validationerror-class="govuk-form-group--error">
                <h1 class="govuk-label-wrapper">
                    <label class="govuk-label govuk-label--l" asp-for="EmployerName">
                        Who is the employer?
                    </label>
                    <span id="employer-name-hint" class="govuk-hint">
                        Start typing their business name
                    </span>

                </h1>
                <span class="govuk-error-message" asp-validation-for="EmployerName"></span>

                <select id="EmployerName" name="EmployerName" class="govuk-input govuk-input" asp-for="EmployerName"></select>

                <input asp-for="SelectedEmployerId" type="hidden" />

                <input id="employerNamesWithIds" type="hidden" />

            </div>

            <button id="tl-continue" class="govuk-button">Continue</button>
        </form>
    </div>
</div>
@section jsfiles{
    <script src="~/javascripts/employer-search.min.js"></script>
}